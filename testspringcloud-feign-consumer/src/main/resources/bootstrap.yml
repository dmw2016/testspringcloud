spring:
  application:
    name: feign-consumer
  zipkin:
    base-url: http://localhost:9411/  # zipkin服务器的地址
    sender:
      type: web # 设置使用http的方式传输数据
  sleuth:
    sampler:
      probability: 1